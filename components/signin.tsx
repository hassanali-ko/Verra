'use client';
import { useState } from 'react';
export function SignIn(){const [busy,setBusy]=useState(false),[message,setMessage]=useState('');return <form onSubmit={async event=>{event.preventDefault();setBusy(true);try{const response=await fetch('/auth/signin',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:new FormData(event.currentTarget).get('email')})});const body=await response.json();setMessage(response.ok?'Check your inbox for your personal sign-in link.':body.error);}catch{setMessage('Could not connect. Your email is still here.');}finally{setBusy(false);}}}><label>Your email<input type="email" name="email" autoComplete="email" required/></label><button className="primary" disabled={busy}>{busy?'Sending…':'Send my sign-in link'}</button><p role="status">{message}</p></form>;}
